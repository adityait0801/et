import VideoTitle from "./VideoTitle";

export default {
    title : 'App/Body/Container/MainContainer/VideoTitle',
    component : VideoTitle
}

const Template =(args)=> <VideoTitle {...args}/>

export const Default = Template.bind({})